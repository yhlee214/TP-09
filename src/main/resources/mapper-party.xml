<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-party">

	<select id="getPartyListByUserNo" parameterType="int" resultType="lx.edu.gonggu.to.PartyTO">
		select partyTitle, targetQty, splitDatetime, productName
		from party
		where user_no = #{userNo}
	</select>
	
	<select id="getPartyList" resultType="lx.edu.gonggu.to.Party">
		select * from party
	</select>
	
	<select id="getPartyByPartyNo" parameterType="int" resultType="lx.edu.gonggu.to.Party">
		select * from party
		where party_no = #{partyNo}
	</select>
	
	<insert id="createParty" parameterType="lx.edu.gonggu.to.Party">
		insert into party(PARTY_TITLE, TARGET_QTY, SPLIT_DATETIME, SPLIT_ADDR, SPLIT_PLACE, LONGITUDE, LATITUDE, PRODUCT_NAME, PRODUCT_CONTENT, PRODUCT_PRICE, PRODUCT_IMG, PARTY_ENDDATETIME, ACCUM_QTY, PARTY_STATUS, PARTY_DATETIME, RECENT_UPDATE, MAX_QTY, USER_NO, REGION_NO ) 
		values (#{partyTitle},#{targetQty}, #{splitDatetime}, #{splitAddr}, #{splitPlace}, #{longitude}, #{latitude}, #{longitude}, #{productName}, #{productContent}, #{productPrice}, #{productImg}, #{partyEndDateTime}, #{accumQty}, #{partyDateTime}, #{recentUpdate}, #{maxQty}, #{userNo}, #{regionNo})
	</insert>
	
	<delete id="deleteParty" parameterType="int">
		delete from party
		where party_no = #{partyNo}
	</delete>
</mapper>