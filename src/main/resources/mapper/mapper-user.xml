<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-user">
<insert id="createUser" parameterType="lx.edu.gongu.to.UserTO">
	insert into users(user_id, user_pwd, user_name, user_phone, user_nickname, user_addr) 
		values(#{userId},#{userPwd},#{userName},#{userPhone},#{userNickname},#{userAddr})
</insert>

<select id="findByUserId" parameterType="String" resultType="String">
	select user_id from users where user_id = #{userId}
</select>

<select id="findUserForLogin" parameterType="String" resultType="lx.edu.gongu.to.UserTO">
	select *
		from users
		where user_id = #{userId}
</select>


</mapper>