<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-reply">

	<insert id="createReply" parameterType="lx.edu.gonggu.to.ReplyTO">
		insert into reply (reply_content, reply_datetime, party_no, user_no)
		values (#{replyContent}, now(), #{partyNo}, #{userNo})
	</insert>
	
	<update id="updateReply" parameterType="lx.edu.gonggu.to.ReplyTO">
		update reply
		set reply_content = #{replyContent}
			, reply_datetime = now()
		where reply_no = #{replyNo}
	</update>
	
	<delete id="deleteReply" parameterType="int">
		delete from reply
		where reply_no = #{replyNo}
	</delete>
	
	<select id="getReplyByReplyNo" parameterType="int" resultType="lx.edu.gonggu.to.ReplyTO">
    select * from reply
    where reply_no = #{replyNo}
</select>

<select id="getReplyListByPartyNo" parameterType="int" resultType="lx.edu.gonggu.to.ReplyTO">
    select r.*, u.user_name as userName
    from reply r
    join users u on r.user_no = u.user_no
    where r.party_no = #{partyNo}
    order by r.reply_datetime desc
</select>

</mapper>