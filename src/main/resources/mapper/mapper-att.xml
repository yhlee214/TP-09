<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-att">

	<insert id="createAtt"
		parameterType="lx.edu.gonggu.to.AttendanceTO">
		insert into attendance (att_qty, att_datetime, att_status, party_no,
		user_no)
		values (#{attQty}, now(), '승인대기', #{partyNo}, #{userNo})
	</insert>

	<select id="getAttByPartyNo" parameterType="int"
		resultType="lx.edu.gonggu.to.AttendanceTO">
		select att_no, att_qty, att_datetime, att_status, party_no, user_no
		from attendance
		where party_no = #{partyNo}
	</select>

	<select id="getAttListByUserNo" parameterType="int"
		resultType="lx.edu.gonggu.to.AttendanceTO">
		select att_no,
		att_qty,
		att_datetime,
		att_status,
		party_no,
		from attendance
		where user_no = #{userNo}
	</select>

	<delete id="deleteAtt" parameterType="int">
		delete from attendance
		where att_no = #{attNo}
	</delete>
	
	<select id="getAttByAttNo" parameterType="int" resultType="lx.edu.gonggu.to.AttendanceTO">
		select att_no, att_qty, att_datetime, att_status, party_no, user_no
			from attendance
			where att_no = #{attNo}
	</select>

</mapper>